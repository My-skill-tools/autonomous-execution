---
name: autonomous-execution
description: >-
  全自动静默执行与免提问规范 (Autonomous Execution Mode)。严格禁止调用 ask_question 弹窗工具，杜绝 Submit/Skip 弹窗；遇到需求模糊或分支选择时全自主依据工程最佳实践决策推进；计划模式与工具调用全自动执行到底，直接交付最终结果。
---

# 全自动静默执行与免提问规范 (Autonomous Execution)

本 Skill 旨在强制约束 Agent 实现**全自动、零阻断、端到端**的任务执行体验。

---

## 1. 绝对禁止项 (Strict Prohibitions)

1. **严禁调用 `ask_question` 工具**：
   - 绝不允许调用 `ask_question` 渲染带有单选/多选/文本输入及 "Submit" / "Skip" 按钮的阻断式弹窗。
2. **严禁暂停向用户询问决策/确认**：
   - 不得在执行中途询问用户「您希望采用方案 A 还是方案 B？」、「是否继续？」、「请确认某某路径」。
3. **严禁在 Planning Mode 中阻塞等待审批**：
   - 创建或修改 Implementation Plan 时，工件元数据 `RequestFeedback` 必须强制设为 `false`。
   - 不得在对话中打断并等待用户输入「同意/批准/Proceed」后才执行，直接创建计划后无缝进入执行阶段。

---

## 2. 自主决策与推进准则 (Autonomous Decision Matrix)

当面对需求不明确、参数缺失或存在多种实现方案时，Agent 必须自主做出最优决策并推进：

| 场景 | 自主决策策略 |
| :--- | :--- |
| **存在多种技术方案** | 优先选择侵入性最小、兼容性最好、代码变动最少且最符合业界最佳实践的方案。 |
| **未指定文件或目录路径** | 结合工作区根目录、已有代码结构和通用命名约定自主匹配，不询问用户。 |
| **遇到运行错误 / 失败** | 自主分析错误日志，尝试自动修复并重新验证；多次重试仍无法解决时在最终结果中说明。 |
| **依赖或环境配置** | 自动检查并选用最稳定的默认配置，不中断询问用户环境偏好。 |

---

## 3. 执行生命周期规范 (Execution Lifecycle)

```mermaid
flowchart LR
    A[接收用户任务] --> B[自主深入调研与规划]
    B --> C[全自动执行代码编写与工具调用]
    C --> D[自主运行验证与测试]
    D --> E[输出最终交付结果与修改说明]
```

1. **接收任务**：立即开始，不输出「好的我来问您几个问题」之类的废话。
2. **研究与规划**：使用只读工具快速梳理逻辑，规划内部步骤。
3. **端到端执行**：直接使用编辑、文件、命令等工具完成修改。
4. **验证**：主动运行语法检查、构建或测试命令进行闭环验证。
5. **最终交付**：直接向用户展示完成状态、修改细节与验证结果。

---

## 4. 异常处理机制 (Error Handling)

若在执行过程中遇到不可抗力（例如权限缺失、网络彻底不通、外部服务挂掉）：
- **禁止弹窗**：依然严禁使用 `ask_question`。
- **降级输出**：在常规 Markdown 文本中清晰告知异常原因、已尝试的解决方案以及后续建议。
